请帮我安装并启动当前目录下的 Streamlit 应用 app.py。请严格按下面步骤执行，不要自由发挥；任何不确定的地方停下问我。

## 项目信息（你应该已经在项目根目录里）
- 入口：app.py
- 启动脚本：run_app.sh（仅 macOS/Linux 可用）
- 依赖清单：requirements.txt → streamlit>=1.50, volcengine-python-sdk>=5.0, av>=13
- bin/ 下已附带 cloudflared 二进制：darwin-arm64、darwin-amd64、linux-amd64
- Python 必须 ≥ 3.10（源码使用 `str | None` 联合类型语法，3.9 会语法报错）

先确认你确实在项目根（跨平台写法，三个文件都会被检查）：
```
python -c "import pathlib,sys; missing=[f for f in ('app.py','requirements.txt','run_app.sh') if not pathlib.Path(f).is_file()]; sys.exit('missing: '+','.join(missing)) if missing else print('cwd ok')"
```
若 `python` 还没装/不在 PATH，bash/zsh 用 `ls app.py requirements.txt run_app.sh`，PowerShell 用 `Get-Item app.py, requirements.txt, run_app.sh`（注意 PowerShell 的逗号）。
任何一个文件不存在就停下问我，不要 cd、不要 git clone。

## 关键约束（违反任何一条都会导致启动失败）
1. **必须**用全新 venv 安装依赖。**禁止**直接 pip install 到系统 python、anaconda、conda 或任何已存在的虚拟环境——README 明确警告这会导致 streamlit 命令的 shebang 与 pip 装包指向不同解释器，启动时报 `ModuleNotFoundError: No module named 'av'`。
2. macOS/Linux **必须**用 `./run_app.sh` 启动（脚本里有依赖一致性自检和 ARK_API_KEY 注入逻辑），不要直接 `streamlit run app.py`。Windows 没有 .sh 脚本，按下面步骤手动 streamlit run。
3. 不要修改 app.py、tunnel.py、run_app.sh、requirements.txt 任何一行。
4. 不要 `git init`、不要重新 clone，代码已就位。
5. 不要把 APIKEY 内容写进会被 git 跟踪的文件（`.gitignore` 已忽略 APIKEY）。
6. macOS/Linux 上**不要**单独安装 cloudflared，bin/ 里已有；**不要** `brew install ffmpeg`，PyAV 自带。

## 第一步：识别平台 & 选定 Python 解释器

判断当前操作系统并选好一个 Python ≥ 3.10 的非 conda 解释器：

**macOS / Linux** —— 在 bash/zsh 里依次尝试，用第一个可用且版本 ≥ 3.10 的：
```
command -v python3.12 || command -v python3.11 || command -v python3.10 || command -v python3
```
对找到的解释器跑一次 `--version` 确认。**绝对不要**使用任何路径包含 `anaconda`、`miniconda`、`conda` 的 python——若候选只剩这一种，停下告诉我，让我装 Homebrew/官方 Python。

**Windows** —— 在 PowerShell 里：
```
py -3.12 --version  # 或 py -3.11 / py -3.10 / py -3
```
找到候选版本后，**必须**用下面这条做硬校验，失败就换一个：
```
py -3.12 -c "import sys; assert sys.version_info >= (3,10), sys.version; print('py ok', sys.version)"
```
（命令里的 `-3.12` 换成你实际选定的版本号。）若所有候选都 < 3.10，停下告诉我。同样跳过 anaconda/conda 路径。

## 第二步：建并激活 venv

**macOS / Linux**：
```
<上一步选定的 python> -m venv .venv
source .venv/bin/activate
which python    # 必须输出 .../.venv/bin/python
which pip       # 必须输出 .../.venv/bin/pip
```

**Windows (PowerShell)**：
```
py -3.12 -m venv .venv     # py 后面跟你上一步确认的版本号
.\.venv\Scripts\Activate.ps1
where.exe python           # 必须包含 .venv\Scripts\python.exe
where.exe pip              # 必须包含 .venv\Scripts\pip.exe
```
若 `Activate.ps1` 报"因为在此系统上禁止运行脚本"，执行一次（仅当前用户）：
```
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```
然后重试激活。

激活后的 which/where 输出不在 .venv 里，就是激活失败，停下排查，不要继续。

## 第三步：装依赖
```
pip install --upgrade pip
pip install -r requirements.txt
```
PyAV 会拉一个 30–60 MB 的 wheel（已内置 ffmpeg），属正常。
卡在 av/PyAV 编译失败：先确认 pip 已升级；再失败就告诉我具体报错，**不要**尝试装系统 ffmpeg。

## 第四步：解释器一致性自检
```
python -c "import av, streamlit, volcenginesdkarkruntime; print('deps ok')"
```
**macOS / Linux**：`which streamlit` 必须在 `.venv/bin/streamlit`
**Windows**：`where.exe streamlit` 必须包含 `.venv\Scripts\streamlit.exe`

任一失败 → 删除 .venv（`rm -rf .venv` 或 `Remove-Item -Recurse -Force .venv`）回到第二步重做。

## 第五步：Windows 才需要装 cloudflared（macOS/Linux 跳过）

仅 Windows：
```
winget install --id Cloudflare.cloudflared
```
装完**重开一个 PowerShell 窗口**让 PATH 生效，然后回到项目目录、重新激活 venv，验证：
```
cloudflared --version
```
没有 winget / 不想用：从 https://github.com/cloudflare/cloudflared/releases 下载 `cloudflared-windows-amd64.exe`，放到项目的 `bin\` 目录下。

## 第六步：跳过 API Key 配置

**不要**问我要 API Key、不要创建 APIKEY 文件、不要设置 ARK_API_KEY 环境变量。直接进入下一步启动即可——应用启动后我会在浏览器侧边栏的 API Key 输入框里手动粘贴，应用会自动保存到 api_keys.json。
（如果项目根已经有 APIKEY 文件，run_app.sh 会自动读取，无需你处理；这一步对你来说就是 no-op。）

## 第七步：启动

**macOS / Linux**（后台运行，输出落日志方便回查）：
```
nohup ./run_app.sh > app.log 2>&1 &
echo $! > app.pid
```

**Windows (PowerShell)**（前台启动一个新窗口跑；显式传 `-WorkingDirectory` 防止新窗口落到 System32）：
```
Start-Process powershell -WorkingDirectory $PWD -ArgumentList '-NoExit','-Command',"& { Set-Location -LiteralPath '$($PWD.Path)'; .\.venv\Scripts\Activate.ps1; streamlit run app.py --server.address 0.0.0.0 --server.port 8501 }"
```

## 第八步：验证启动成功

等约 8 秒让 streamlit + Cloudflare Tunnel 起来，然后：

**macOS / Linux**：
```
sleep 8
curl -sI http://localhost:8501 | head -1
```
**Windows**：
```
Start-Sleep -Seconds 8
(Invoke-WebRequest -Uri http://localhost:8501 -UseBasicParsing).StatusCode
```
期望：HTTP 200。否则查日志（macOS/Linux: `tail -50 app.log`；Windows: 看启动窗口里的 traceback）。

确认 Cloudflare Tunnel URL 已生成（macOS/Linux）：
```
grep -oE 'https://[a-z0-9-]+\.trycloudflare\.com' logs/cloudflared.log | tail -1
```
Windows 路径是 `logs\cloudflared.log`，可用 `Select-String 'trycloudflare\.com' logs\cloudflared.log | Select-Object -Last 1`。
首次启动可能要再等几秒该日志文件才出现。

## 第九步：报告

最后用一段话告诉我：
- 本地访问地址：http://localhost:8501
- Cloudflare Tunnel URL（上一步抓到的，浏览器侧边栏也会显示）
- 进程 PID 或启动窗口位置
- 日志文件路径
- 如何停止：
  - macOS/Linux: `kill $(cat app.pid)`
  - Windows: 关掉启动 streamlit 的那个 PowerShell 窗口，或 `Get-Process streamlit | Stop-Process`

## 兜底
- 端口 8501 被占：macOS/Linux 用 `PORT=8502 nohup ./run_app.sh > app.log 2>&1 &`；Windows 把启动命令里的 `--server.port 8501` 改成 8502
- Cloudflare Tunnel 30 秒超时：本机网络访问不到 `*.trycloudflare.com`，告诉我，可能要走代理，不要重试更多次
- 出现 `ModuleNotFoundError: No module named 'av'`：100% 是 venv 没激活/被串了，删掉 .venv 回到第二步
- UI 报 `401 AuthenticationError`：属于正常状态，去浏览器侧边栏 API Key 输入框粘贴 ark-xxx 即可，不需要你处理

执行过程中任何不确定的事，停下问我，不要乱猜。