# 本地启动与配置指南（平台适配版）

本项目 fork 自 [8sm7chw9pt-bit/seedance-streamlit](https://github.com/8sm7chw9pt-bit/seedance-streamlit)（无许可证，仅内部调试使用，勿二次分发），已适配皖美平台视频透传网关。

## 一、已完成的改造（一次性，无需重复）

| 项 | 内容 |
|---|---|
| app.py 模型表 | 换成平台在售模型 `doubao-seedance-2.5` / `doubao-seedance-2.0`（dev 库 PUBLISHED） |
| app.py base_url | `get_client(api_key, base_url)` 支持自定义网关；侧边栏新增「API Base URL」输入框（也支持 `ARK_BASE_URL` 环境变量）；分辨率补 1080p |
| tunnel.py | 修复 Windows 下 winget 安装的 cloudflared 找不到的 bug（原代码 `Path("cloudflared").exists()` 只查工作目录不查 PATH，改用 `shutil.which`） |
| 依赖 | `uv add httpx pydantic`——火山全家桶 SDK 依赖声明缺这两个，已写入 pyproject.toml + uv.lock |
| cloudflared | winget 安装，位置 `C:\Program Files (x86)\cloudflared\cloudflared.exe`（2026.9.1） |

## 二、前置条件（每次使用前）

1. **后端起着**：本地联调需 gateway(8080) + cloud-business(9206)；或把 Base URL 指向 dev 网关（地址由运维提供）。
2. **业务 Key**：`sk-wm-` 开头，console 客户控制台签发。

## 三、启动流程

> ⚠️ 必须**新开一个终端**——cloudflared 是 winget 装的，PATH 写在注册表，装它之前就开着的终端看不到。

PowerShell：

```powershell
cd E:\TydicCode\13AISupermarket\seedance-streamlit
.\.venv\Scripts\Activate.ps1
streamlit run app.py --server.address 127.0.0.1 --server.port 8501 --server.headless true
```

浏览器打开 http://127.0.0.1:8501

- `127.0.0.1` = 仅本机访问；要手机/局域网访问改成 `0.0.0.0`
- 老终端临时补 PATH：`$env:Path += ";C:\Program Files (x86)\cloudflared"`
- 换新机器：`uv sync` 一键恢复全部依赖（httpx/pydantic 已进 lock）

### PyCharm 里启动（Run Configuration）

**不能用默认的 `python app.py` 裸跑**——Streamlit 应用必须经 `streamlit run` 启动，裸跑下所有 UI 调用是空操作，最终报 `UnboundLocalError: tun`。另外**必须带 `--server.headless true`**：首次运行 Streamlit 会弹邮箱询问（onboarding），PyCharm Run 窗口不是交互终端、无法输入，会卡死在 `Email:` 提示处。

`Run → Edit Configurations → + → Python`：

| 字段 | 值 |
|---|---|
| Module name（把 Script path 切换成 Module name） | `streamlit` |
| Parameters | `run app.py --server.address 127.0.0.1 --server.port 8501 --server.headless true` |
| Working directory | `E:\TydicCode\13AISupermarket\seedance-streamlit` |
| Python interpreter | 项目 `.venv` |

⚠️ 若 PyCharm 是装 cloudflared **之前**打开的，其进程 PATH 里没有 cloudflared，tunnel 会启动失败——重启 PyCharm 即可（或复制 `C:\Program Files (x86)\cloudflared\cloudflared.exe` 到本项目 `bin\cloudflared-windows-amd64.exe` 绕开 PATH）。

## 四、页面配置（侧边栏，自上而下）

| 配置项 | 填什么 |
|---|---|
| API Key | `sk-wm-...`（首次粘贴后存本地 api_keys.json，下次下拉直接选；不进任何服务器） |
| API Base URL | 本地网关 `http://localhost:8080/cloud/video/api/api/v3`（默认值，一般不动）；连远程 dev 填 `http://117.57.203.251:6080/dev-api/cloud/video/api/api/v3`（32000/30833 入口同效；**不要用 `/prod-api` 前缀**——生产库没有视频模型，发不了任务）。**两段 api 是网关前缀+上游版本，缺一段就 404/401** |
| 模型 | 2.5（平台主推，最长 30 秒）/ 2.0（多模态参考输入） |
| 画面/时长 | 比例 6 种；分辨率 1080p/720p/480p（**影响计费档位**）；时长 4–15 秒 |

启动正常的标志：侧边栏底部出现 **绿色圆点 + `Tunnel · xxx.trycloudflare.com`**——没有它拖素材功能不可用（tunnel 失败整个应用会停）。

## 五、验证顺序（首次使用）

1. **先文生视频**：不依赖素材，验证「页面 → 本地网关 → cloud-business → 上游」协议链路。
2. **再图生视频**：拖一张本地图，工具自动经 Quick Tunnel 转成公网 URL 供上游拉取——验证素材链路。
3. 生成成功的视频自动归档到 `videos\{task_id}.mp4`（上游直链 24 小时过期，本地留档不丢）。
4. 「从服务端同步历史」按钮可拉回最近 50 条服务端任务。

## 六、计费提醒

**每次创建任务都走平台真实计费链路**（创建即预冻结、终态结算多退少补）。调试期建议：短时长 + 480p，别用大额度 Key。

## 七、常见问题

| 症状 | 原因与解决 |
|---|---|
| Cloudflare Tunnel 启动失败 | 老终端没有 cloudflared PATH → 新开终端；或出口网络不通 *.trycloudflare.com；日志见 `logs\cloudflared.log` |
| 401 token 无效 | Key 未填/填错；或 Base URL 填错（两段 api 丢一段） |
| 连接拒绝/超时 | 本地网关没起（确认 8080 的 gateway 与 9206 的 cloud-business 都在） |
| 图生视频报 InvalidParameter.URL | tunnel 域名已变（应用重启过）——重新拖一次图生成新 URL 即可 |
| 上游报错看不懂 | 提交卡片会展开上游 error code/message，对照《视频接口使用说明.md》§8 错误语义对照表 |
